{
  "name": "@my-app/api",
  "version": "0.0.1",
  "private": true,
  "lint": "eslint .",
  "scripts": {
    "dev": "ts-node-dev --respawn src/index.ts",
    "build": "tsc",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@my-app/config": "workspace:*",
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
